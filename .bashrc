# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions

alias cds="cd /scr/grstephe"
#alias cdd  'cd `echo $cwd | awk -f ~/bin/cdw.awk`'
#alias cds  'cd `echo $cwd | awk -f ~/bin/cdw.awk`'
#alias cdw  'cd `echo $cwd | awk -f ~/bin/cdw.awk`'
alias go22="cd /p/home/grstephe/hycom/GLBc0.04/expt_22.1"
alias go2w="cd /p/work1/grstephe/hycom/GLBc0.04/expt_22.1"
